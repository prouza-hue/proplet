# Technical debt refactor status

- Sprint: 07B — Read-only stats a explicitní repair command
- Branch: `refactor/s07b-readonly-stats`
- Base SHA: `f3ec900` (`origin/main`, schválený a produkčně ověřený Sprint 07A)
- Current code checkpoint: `d83ac51` lokálně; remote preview squash `4ac6fb5` (`Sprint 07B: read-only stats and explicit repair command`)
- Stav: hotovo; povinný STOP po samostatném Sprintu 07B
- Zamýšlená změna chování: GET/read výpočet statistik již nesmí volat repair ani zapisovat do DB. Pro konzistentní data zůstává response stejná. Nekonzistentní historické řádky se pouze detekují bez PII; oprava bude dostupná jen jako explicitní idempotentní command s výchozím dry-run režimem.
- Změněné soubory: `backend/progress.py`, `server.py`, `tools/repair_gen4_rewards.py`, `tools/test_v4010_gen4_xp_rewards.py`, `tools/test_xp_economy_v40132.py`, `tests/current/test_s07a_progress_pure_core.py`, `tests/current/test_s07b_readonly_stats.py`, `tests/current/manifest.json`, tento statusový dokument.
- Hotové kroky: Zmapován jediný hidden write `player_stats → reconcile → db_update(results.points)` a jeho read/write callers (`/api/me`, export, admin detail, leaderboard, auth payloady, result/rescue response). Původní side effect zachycen v samostatném characterization commitu `f7059d4`; side-effectful interní helper byl záměrně odstraněn, aby jej žádný další read caller nemohl znovu použít. Repair pravidla jsou nyní čistý deterministický plán; `player_stats()` pouze čte a pro konzistentní fixture zachovává úplný pre-change response hash `9edb97ea…`. Nekonzistentní fixture vrací persisted-only XP a pouze anonymní agregovaný log. Explicitní CLI je defaultně dry-run; apply vyžaduje potvrzovací token, používá compare-and-set a odděluje planned/applied XP od konfliktů. Přidány testy dry-run, opakovaného apply, CAS konfliktu, schema-invalid points, částečného multi-row apply + retry, pre-Gen4 short-circuitu a skutečného `/api/result` vs. stats interleavingu.
- Zbývající kroky: Žádné v rozsahu 07B. Nemergovat do `main` bez uživatelského ověření preview a výslovného pokynu.
- Testy PASS: baseline current gate 26/26; finální current gate 27/27, assety 72/72, syntax 201/201; cílené Sprint 07A/07B a Gen4 reward testy PASS; CLI `--apply` bez přesného potvrzení končí před DB přístupem; `git diff --check` PASS. Nezávislé review uzavřelo pre-Gen4, CAS, planned/applied report, partial retry, parity i skutečný `/api/result` interleaving bez zbývajícího runtime blockeru. Preview deployment je READY; `/`, `/api/config`, `/api/health` vracejí 200, health má `ok: true`, `database: true`, Vercel status je success a po smoke nejsou error/fatal runtime logy.
- Testy FAIL / nespouštěné: Žádný current test neselhal. Historický mimo-gate `tools/test_xp_economy_v40132.py` nadále selhává na předem existující source-string asserci frontendového bonusu; relevantní stats část před ní prochází. Produkční DB ani schema verification SQL se nespouštěly a nespustí; explicitní repair command nebyl spuštěn proti žádné reálné DB.
- Nově nalezená rizika: Nekonzistentní historický profil nově až do explicitní opravy zobrazuje skutečně uložené nižší XP; leaderboard už taková data neopravuje při čtení. Skutečný počet dotčených uživatelů nebyl zjišťován, protože by vyžadoval práci s produkčními daty. Apply command není jedna DB transakce, ale každý PATCH je CAS a celý command je bezpečně opakovatelný; částečný běh je viditelný oddělenými `plannedXp`/`appliedXp`/`conflicts` a další běh replanuje z uloženého stavu. Produkční schema garantuje nezáporný non-null integer `results.points`; command přesto neplatné importované hodnoty odmítne a reportuje bez zápisu.
- Bezpečný bod pokračování: preview branch `refactor/s07b-readonly-stats`, ověřený runtime squash `4ac6fb5`; lokálně samostatné logické commity pro characterization, implementaci, pre-Gen4 parity a review hardening. Veřejná route/OpenAPI plocha, schema, content, gameplay i frontend zůstávají beze změny.
- Další povolený sprint: žádný. Sprint 08A se nesmí zahájit bez dokončení, preview kontroly a nového výslovného pokynu po STOP Sprintu 07B.
